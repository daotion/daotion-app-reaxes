import { initial } from "lodash";

export const reaxel_payin_order = function(){
	const initialState = {
		input_search_text : "",
		
	};
	const { store , setState } = orzMobx({});
	return () => {
		
		return {
			
		};
	};
}();
